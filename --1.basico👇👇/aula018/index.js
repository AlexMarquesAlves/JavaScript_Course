// function saudacao(nome) {
//   return `Bom Dia ${nome}!`;
// }

// const variavel = saudacao("Leo");
// console.log(variavel);

function soma(x, y) {
  const resultado = x + y;
  return resultado;
}
